{
  "name": "range-inviter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lodash": "4.17.5"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "node app.js",
    "pretest": "npm install",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "23.5.0"
  }
}
